find() 方法在部分匹配时和完全匹配时返回true,匹配部分时就截取匹配的;

这个可以理解为，比如我只匹配到了表达式的前面 中间 后面中的某一个，那我就截取匹配的字符串，该方法条件不严格。

matches() 方法只有在完全匹配时返回true，该方法条件严格,在zookeeper一般多用于匹配命令。

表达式中包含一对括号时,

group(0) 代表原始字符串

group(1) 代表 () 中匹配的字符串
